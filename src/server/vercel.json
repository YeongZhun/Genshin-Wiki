{
  "version": 2,
  "builds": [
    {
      "src": "./Backend.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "routes": [{ "src": "/[^.]+", "dest": "/", "status": 200 }]
    }
  ]
}